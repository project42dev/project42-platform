# Commands

```powershell
npm ci
npm run check
npm run api:check
npm run db:migrate:local
```

Remote D1 migration and Worker deployment require private production
configuration and are performed from the operations release process.
