# Gotchas

- `wrangler.jsonc` contains local placeholders only; never replace them with
  hosted Project 42 identifiers in this public repository.
- D1 audit rows are immutable except for the narrow null-only privacy redaction
  performed during verified account deletion.
- A bootstrap owner must be identified by issuer and subject before production
  registration is opened.
