import { validateFixture } from '../src/validate.mjs';
function referenceQuantile(buckets,q){const total=buckets.at(-1).count;const target=q*total;let lowCount=0;let lowBound=0;for(const high of buckets){if(target<=high.count){const population=high.count-lowCount;return population===0?high.le:lowBound+((target-lowCount)*(high.le-lowBound))/population}lowCount=high.count;lowBound=high.le}return buckets.at(-1).le}
export function referenceAggregate(input){const fixture=validateFixture(input);const bounds=fixture.records[0].buckets.map(bucket=>bucket.le);const totals=bounds.map(()=>0);for(const record of fixture.records)for(let i=0;i<bounds.length;i+=1)totals[i]+=record.buckets[i].count;const merged=bounds.map((le,i)=>({le,count:totals[i]}));return {p95Ms:referenceQuantile(merged,.95),requests:totals.at(-1),replicas:fixture.records.length,releaseId:fixture.identity.releaseId}}
