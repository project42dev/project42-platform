# Make Model Operations Observable without Making Data Public: reduced-motion presentation

Present every visual as a complete static composition. Do not make completion
depend on animation timing, autoplay, or pointer gestures.

## welcome-and-outcomes

Show the objectives simultaneously as a static text panel.

Text alternative: The lesson covers questions, correlated identities, privacy-conscious telemetry, distributions and cost, and actionable alerts.

## questions-objectives-narration

Display the entire mapping as a static table and emphasize rows with text and borders.

Text alternative: Rows cover exact model identity, valid outcomes, failing slices, queueing, saturation, release change, cost, recovery, and incident scope.

## correlation-narration

Show all correlated rows simultaneously as a static table.

Text alternative: Gateway, adapter, server, runtime, policy, resource, and outcome rows share an identifier and exact release identity without storing prompt content.

## correlation-demonstration

Present the diagnosis as a numbered static evidence table.

Text alternative: Metadata and exact identities support diagnosis and recovery while raw prompt and response content remain outside routine telemetry.

## telemetry-privacy-narration

Display the lifecycle as a static table with four field classes.

Text alternative: Secrets and routine raw content are prohibited; bounded metadata and derived measures are preferred; approved samples receive strict controls.

## distributions-cost-narration

Provide exact histogram bucket counts and cost values in labeled static tables.

Text alternative: Underlying histogram observations aggregate across replicas; precomputed p95 values are not averaged. Queue, first-output, processing, streaming, saturation, and cost are separate.

## histogram-demonstration

Show all equations and values in static selectable text.

Text alternative: The arrays sum to 56, 107, 143, and 150. The estimated p95 is 397.22 milliseconds. Cost totals 3.75 dollars, or 0.00025 dollars per completed request.

## alerts-investigation-narration

Use a static alert contract table with every response field visible.

Text alternative: Alerts cover objective burn, policy-deny anomaly, queue saturation, identity mismatch, and telemetry loss or cost anomaly.

## operator-contract-worked-example-lab-narration

Display the ten rows and contract definitions as static selectable text.

Text alternative: The contract includes build, outcome, slice, timing, queue, saturation, quality, cost, recovery, and incident evidence with privacy and cardinality controls.

## repair-lab-lab-narration

Show the inputs, commands, restrictions, and expected outputs as static selectable text.

Text alternative: Learners inspect baseline and changed cumulative counts, predict before execution, edit one implementation file, and compare supplied starter and repaired outputs.

## class-closing

Show the closing rule as static text with no animation.

Text alternative: Use bounded evidence for accountable operational decisions. Synthetic lab results do not prove production behavior.
