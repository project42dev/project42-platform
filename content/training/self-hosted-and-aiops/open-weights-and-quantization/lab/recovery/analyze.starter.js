// Recovery copy of the deliberate starter defect.
// Copy this file to src/analyze.js only if you need to restart the exercise.
export { validateManifest, validateVariant, predictedPayloadBytes, analyzeVariant, analyzeManifest } from "../src/analyze.js";

// Deliberate defective algorithm shown for recovery review:
export function residentWeightBytesForRuntime(variant, payloadBytes) {
  return payloadBytes;
}
