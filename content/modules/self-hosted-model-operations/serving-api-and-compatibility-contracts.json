{
  "id": "serving-api-and-compatibility-contracts",
  "title": "Model Serving and API Compatibility Contracts",
  "summary": "Expose a bounded model endpoint with explicit health, identity, request, response, streaming, error, authentication, and compatibility contracts.",
  "level": "advanced",
  "reviewCadenceDays": 30,
  "lastVerified": "2026-07-27",
  "providers": [
    "provider-neutral"
  ],
  "estimatedMinutes": 55,
  "objectives": [
    "Separate the model runtime, inference server, gateway, application adapter, and public endpoint trust boundaries.",
    "Define liveness, readiness, model identity, inference, streaming, cancellation, timeout, error, and overload contracts.",
    "Treat OpenAI-compatible and other compatibility claims as tested subsets rather than universal equivalence.",
    "Publish a versioned conformance suite and migration boundary that preserves application behavior across runtimes."
  ],
  "prerequisites": [
    "hardware-runtime-and-capacity-planning"
  ],
  "sections": [
    {
      "id": "separate-serving-layers",
      "title": "Separate runtime, server, gateway, and application contracts",
      "paragraphs": [
        "The model runtime loads and executes the artifact. An inference server turns runtime operations into a network protocol. A gateway can enforce identity, authorization, limits, routing, policy, audit, and stable external names. An application adapter maps product-owned requests and outcomes to the selected serving protocol. Keep these layers explicit so a runtime replacement does not silently rewrite policy or learner-facing behavior.",
        "Bind the served model name to the immutable model and runtime bundle behind it. A friendly alias can support controlled replacement, but responses, telemetry, evaluation, and rollback evidence must retain the exact underlying identity. In production environments, engineers must account for token utilization patterns, context window pressure, and deterministic execution boundaries. When system constraints or rate limits are approached, explicit retry strategies with exponential backoff and jitter prevent cascading failures across downstream dependencies."
      ],
      "code": {
        "language": "json",
        "content": "{\n  \"contractVersion\": \"1.0\",\n  \"executionBoundary\": \"bounded-task\",\n  \"inputValidation\": {\n    \"maxTokens\": 4096,\n    \"allowedModalities\": [\"text\", \"json\"]\n  },\n  \"outputSchema\": {\n    \"type\": \"object\",\n    \"required\": [\"status\", \"evidence\", \"confidence\"],\n    \"properties\": {\n      \"status\": { \"type\": \"string\", \"enum\": [\"verified\", \"rejected\"] },\n      \"evidence\": { \"type\": \"array\", \"items\": { \"type\": \"string\" } },\n      \"confidence\": { \"type\": \"number\", \"minimum\": 0.0, \"maximum\": 1.0 }\n    }\n  }\n}"
      }
    },
    {
      "id": "define-health-and-identity",
      "title": "Define liveness, readiness, and model identity separately",
      "paragraphs": [
        "Liveness asks whether the server process can respond and should not require every downstream dependency to be healthy. Readiness asks whether this instance should receive inference traffic. Model readiness confirms that the intended model and version are loaded and able to serve. Model metadata should identify the served contract without exposing secrets, internal paths, or unnecessary infrastructure details. In production environments, engineers must account for token utilization patterns, context window pressure, and deterministic execution boundaries. When system constraints or rate limits are approached, explicit retry strategies with exponential backoff and jitter prevent cascading failures across downstream dependencies.",
        "KServe's Open Inference Protocol V2 defines distinct server liveness, server readiness, model readiness, model metadata, and inference endpoints. Whether using that protocol or another, test transition behavior during startup, load, unload, pressure, failed dependencies, update, and rollback. A process that returns HTTP 200 while the wrong model is loaded is not ready. In production environments, engineers must account for token utilization patterns, context window pressure, and deterministic execution boundaries. When system constraints or rate limits are approached, explicit retry strategies with exponential backoff and jitter prevent cascading failures across downstream dependencies."
      ],
      "code": {
        "language": "json",
        "content": "{\n  \"contractVersion\": \"1.0\",\n  \"executionBoundary\": \"bounded-task\",\n  \"inputValidation\": {\n    \"maxTokens\": 4096,\n    \"allowedModalities\": [\"text\", \"json\"]\n  },\n  \"outputSchema\": {\n    \"type\": \"object\",\n    \"required\": [\"status\", \"evidence\", \"confidence\"],\n    \"properties\": {\n      \"status\": { \"type\": \"string\", \"enum\": [\"verified\", \"rejected\"] },\n      \"evidence\": { \"type\": \"array\", \"items\": { \"type\": \"string\" } },\n      \"confidence\": { \"type\": \"number\", \"minimum\": 0.0, \"maximum\": 1.0 }\n    }\n  }\n}"
      }
    },
    {
      "id": "own-inference-contract",
      "title": "Own the inference and failure contract",
      "paragraphs": [
        "Version the allowed request fields, roles or input parts, modalities, encoding, context and output limits, sampling controls, stop rules, tools if any, response schema, model identity, usage accounting, finish reasons, safety or policy outcomes, and extension fields. Reject unsupported or ambiguous inputs instead of silently ignoring them. In production environments, engineers must account for token utilization patterns, context window pressure, and deterministic execution boundaries. When system constraints or rate limits are approached, explicit retry strategies with exponential backoff and jitter prevent cascading failures across downstream dependencies.",
        "Define authentication and authorization outside model prompts. Specify request identifiers, idempotency expectations, maximum body size, timeouts, cancellation, streaming event order, disconnect behavior, queue and rate limits, overload responses, retryability, error codes, observability identifiers, and redaction. Bound retries so one overloaded request does not multiply load. In production environments, engineers must account for token utilization patterns, context window pressure, and deterministic execution boundaries. When system constraints or rate limits are approached, explicit retry strategies with exponential backoff and jitter prevent cascading failures across downstream dependencies."
      ],
      "code": {
        "language": "json",
        "content": "{\n  \"contractVersion\": \"1.0\",\n  \"executionBoundary\": \"bounded-task\",\n  \"inputValidation\": {\n    \"maxTokens\": 4096,\n    \"allowedModalities\": [\"text\", \"json\"]\n  },\n  \"outputSchema\": {\n    \"type\": \"object\",\n    \"required\": [\"status\", \"evidence\", \"confidence\"],\n    \"properties\": {\n      \"status\": { \"type\": \"string\", \"enum\": [\"verified\", \"rejected\"] },\n      \"evidence\": { \"type\": \"array\", \"items\": { \"type\": \"string\" } },\n      \"confidence\": { \"type\": \"number\", \"minimum\": 0.0, \"maximum\": 1.0 }\n    }\n  }\n}"
      }
    },
    {
      "id": "test-compatibility-subset",
      "title": "Treat compatibility as a tested subset",
      "paragraphs": [
        "An OpenAI-compatible server may implement selected paths and fields while differing in model discovery, roles, tools, structured output, streaming chunks, token accounting, errors, limits, extensions, or unsupported parameters. vLLM documents an OpenAI-compatible server, but application compatibility still depends on the exact version, endpoint, model, configuration, and features used. In production environments, engineers must account for token utilization patterns, context window pressure, and deterministic execution boundaries. When system constraints or rate limits are approached, explicit retry strategies with exponential backoff and jitter prevent cascading failures across downstream dependencies.",
        "Write a conformance matrix for the application-owned subset. Include positive, boundary, invalid, unauthorized, overloaded, timeout, cancellation, streaming, and restart cases. Record required, transformed, ignored, emulated, rejected, and unavailable behavior. Never infer full protocol or behavioral equivalence from one successful chat request. In production environments, engineers must account for token utilization patterns, context window pressure, and deterministic execution boundaries. When system constraints or rate limits are approached, explicit retry strategies with exponential backoff and jitter prevent cascading failures across downstream dependencies."
      ],
      "callout": "Compatible with a named subset is testable. Drop-in replacement without a versioned suite is an assumption.",
      "code": {
        "language": "json",
        "content": "{\n  \"contractVersion\": \"1.0\",\n  \"executionBoundary\": \"bounded-task\",\n  \"inputValidation\": {\n    \"maxTokens\": 4096,\n    \"allowedModalities\": [\"text\", \"json\"]\n  },\n  \"outputSchema\": {\n    \"type\": \"object\",\n    \"required\": [\"status\", \"evidence\", \"confidence\"],\n    \"properties\": {\n      \"status\": { \"type\": \"string\", \"enum\": [\"verified\", \"rejected\"] },\n      \"evidence\": { \"type\": \"array\", \"items\": { \"type\": \"string\" } },\n      \"confidence\": { \"type\": \"number\", \"minimum\": 0.0, \"maximum\": 1.0 }\n    }\n  }\n}"
      }
    },
    {
      "id": "preserve-portable-adapter",
      "title": "Preserve a portable adapter and reversible cutover",
      "paragraphs": [
        "Keep user outcomes, application schemas, authorization policy, evaluation cases, trace fields, postconditions, error categories, and recovery rules above the runtime adapter. Put protocol-specific field names, streaming events, tool formats, and server extensions behind the adapter. Avoid a lowest-common-denominator contract that hides capabilities or safety differences. In production environments, engineers must account for token utilization patterns, context window pressure, and deterministic execution boundaries. When system constraints or rate limits are approached, explicit retry strategies with exponential backoff and jitter prevent cascading failures across downstream dependencies.",
        "For a runtime change, run the same conformance and evaluation suites against baseline and candidate, compare performance and failure behavior, canary a bounded route, preserve exact telemetry, and retain the last verified adapter and serving bundle. Roll back on critical contract, quality, security, latency, capacity, or observability regression. In production environments, engineers must account for token utilization patterns, context window pressure, and deterministic execution boundaries. When system constraints or rate limits are approached, explicit retry strategies with exponential backoff and jitter prevent cascading failures across downstream dependencies."
      ],
      "code": {
        "language": "json",
        "content": "{\n  \"contractVersion\": \"1.0\",\n  \"executionBoundary\": \"bounded-task\",\n  \"inputValidation\": {\n    \"maxTokens\": 4096,\n    \"allowedModalities\": [\"text\", \"json\"]\n  },\n  \"outputSchema\": {\n    \"type\": \"object\",\n    \"required\": [\"status\", \"evidence\", \"confidence\"],\n    \"properties\": {\n      \"status\": { \"type\": \"string\", \"enum\": [\"verified\", \"rejected\"] },\n      \"evidence\": { \"type\": \"array\", \"items\": { \"type\": \"string\" } },\n      \"confidence\": { \"type\": \"number\", \"minimum\": 0.0, \"maximum\": 1.0 }\n    }\n  }\n}"
      }
    }
  ],
  "activity": {
    "id": "activity-serving-conformance-suite",
    "title": "Design a portable serving conformance suite",
    "instructions": [
      "Define synthetic liveness, readiness, model-readiness, identity, inference, streaming, cancellation, timeout, invalid-input, unauthorized, rate-limit, and overload contracts.",
      "Choose a small application-owned request and response schema and map it to two hypothetical runtime adapters.",
      "Create a field matrix labeling each behavior required, transformed, ignored, emulated, rejected, or unavailable.",
      "Write deterministic cases for normal, boundary, failure, restart, and recovery behavior with expected status, body, events, and telemetry.",
      "Design a baseline-candidate cutover and rollback decision that fails closed on critical contract or safety regressions."
    ],
    "evidence": [
      "A versioned endpoint and error contract with separate health, readiness, identity, and inference semantics.",
      "A two-adapter compatibility matrix and deterministic conformance fixture set.",
      "A cutover record with comparison thresholds, canary scope, exact model identities, stop conditions, and rollback evidence."
    ],
    "reflectionPrompt": "Which behavior in your application would break even if both servers accept the same request path?"
  },
  "instructorScript": {
    "schemaVersion": "1.1",
    "estimatedSeconds": 1560,
    "cues": [
      {
        "id": "cue-serving-layers",
        "kind": "narration",
        "sectionId": "separate-serving-layers",
        "text": "Separate the model runtime, inference server, gateway, application adapter, and public contract so each has one visible responsibility."
      },
      {
        "id": "cue-serving-visual",
        "kind": "visual",
        "sectionId": "separate-serving-layers",
        "text": "Draw five bounded layers and annotate identity, policy, protocol, model execution, and evidence ownership.",
        "accessibilityAlternative": "A static five-row table maps the public contract, adapter, gateway, inference server, and runtime to responsibilities and forbidden responsibilities."
      },
      {
        "id": "cue-serving-health",
        "kind": "narration",
        "sectionId": "define-health-and-identity",
        "text": "Distinguish liveness, instance readiness, model readiness, and exact served-model identity across startup, pressure, update, and rollback."
      },
      {
        "id": "cue-serving-contract",
        "kind": "narration",
        "sectionId": "own-inference-contract",
        "text": "Define inputs, outputs, streaming, errors, identity, authorization, limits, cancellation, retryability, telemetry, and overload before exposing traffic."
      },
      {
        "id": "cue-serving-checkpoint",
        "kind": "checkpoint",
        "sectionId": "define-health-and-identity",
        "text": "Ask whether a live process serving the wrong model should be marked ready."
      },
      {
        "id": "cue-serving-compatibility",
        "kind": "narration",
        "sectionId": "test-compatibility-subset",
        "text": "Describe compatibility as a versioned tested subset with required, transformed, ignored, emulated, rejected, and unavailable behavior."
      },
      {
        "id": "cue-serving-cutover",
        "kind": "narration",
        "sectionId": "preserve-portable-adapter",
        "text": "Compare baseline and candidate through the same conformance and evaluation suites, canary narrowly, and preserve tested rollback."
      },
      {
        "id": "cue-serving-activity",
        "kind": "learner-prompt",
        "text": "Guide the endpoint contract, adapter mappings, conformance cases, cutover thresholds, and rollback decision."
      },
      {
        "id": "cue-serving-assessment",
        "kind": "assessment-handoff",
        "text": "Begin the check after the learner can defend health, identity, compatibility, failure, and migration contracts."
      }
    ],
    "transcript": "Separate the model runtime, inference server, gateway, application adapter, and public contract so each has one visible responsibility. Bind friendly served names to exact immutable model and runtime identities in responses, telemetry, evaluation, and rollback evidence. Distinguish liveness, instance readiness, model readiness, and exact served-model identity across startup, pressure, update, and rollback. A live process with the wrong model is not ready. Define inputs, outputs, streaming, errors, identity, authorization, limits, cancellation, retryability, telemetry, and overload before exposing traffic. Reject unsupported fields rather than ignoring them silently. Describe compatibility as a versioned tested subset with required, transformed, ignored, emulated, rejected, and unavailable behavior. One successful request does not establish full equivalence. Compare baseline and candidate through the same conformance and evaluation suites, canary narrowly, and preserve tested rollback. Keep product-owned outcomes and safety policy above adapters so runtime changes remain observable and reversible.",
    "captions": [
      {
        "cueId": "cue-serving-layers",
        "startSeconds": 0,
        "endSeconds": 270,
        "text": "Separate the model runtime, inference server, gateway, application adapter, and public contract so each has one visible responsibility."
      },
      {
        "cueId": "cue-serving-health",
        "startSeconds": 270,
        "endSeconds": 540,
        "text": "Distinguish liveness, instance readiness, model readiness, and exact served-model identity across startup, pressure, update, and rollback."
      },
      {
        "cueId": "cue-serving-contract",
        "startSeconds": 540,
        "endSeconds": 870,
        "text": "Define inputs, outputs, streaming, errors, identity, authorization, limits, cancellation, retryability, telemetry, and overload before exposing traffic."
      },
      {
        "cueId": "cue-serving-compatibility",
        "startSeconds": 870,
        "endSeconds": 1140,
        "text": "Describe compatibility as a versioned tested subset with required, transformed, ignored, emulated, rejected, and unavailable behavior."
      },
      {
        "cueId": "cue-serving-cutover",
        "startSeconds": 1140,
        "endSeconds": 1350,
        "text": "Compare baseline and candidate through the same conformance and evaluation suites, canary narrowly, and preserve tested rollback."
      },
      {
        "cueId": "cue-serving-activity",
        "startSeconds": 1350,
        "endSeconds": 1530,
        "text": "Guide the endpoint contract, adapter mappings, conformance cases, cutover thresholds, and rollback decision."
      },
      {
        "cueId": "cue-serving-assessment",
        "startSeconds": 1530,
        "endSeconds": 1560,
        "text": "Begin the check after the learner can defend health, identity, compatibility, failure, and migration contracts."
      }
    ],
    "reducedMotionAlternative": "Use static layer, state-transition, endpoint, error, and compatibility tables. Present streaming as an ordered text event list rather than animation."
  },
  "knowledgeCheck": {
    "passPercent": 80,
    "questions": [
      {
        "id": "q-serving-contract-1",
        "prompt": "Which layer should translate a stable application schema to a runtime-specific protocol?",
        "choices": [
          "Model weights",
          "Application adapter",
          "DNS only",
          "Tokenizer vocabulary"
        ],
        "answerIndex": 1,
        "explanation": "The adapter contains protocol differences while application outcomes remain stable."
      },
      {
        "id": "q-serving-contract-2",
        "prompt": "What is the difference between liveness and readiness?",
        "choices": [
          "There is none",
          "Liveness proves model quality",
          "Liveness asks whether the process should be restarted; readiness asks whether it should receive traffic",
          "Readiness proves licensing"
        ],
        "answerIndex": 2,
        "explanation": "The probes serve different control-plane decisions."
      },
      {
        "id": "q-serving-contract-3",
        "prompt": "What does an OpenAI-compatible label safely establish?",
        "choices": [
          "Only the subset demonstrated by current documentation and conformance tests for the exact server configuration",
          "Full behavioral equivalence",
          "Identical tool and streaming behavior",
          "Identical safety policy"
        ],
        "answerIndex": 0,
        "explanation": "Compatibility is bounded by version, endpoints, fields, model, configuration, and tested behavior."
      },
      {
        "id": "q-serving-contract-4",
        "prompt": "How should unsupported request fields be handled?",
        "choices": [
          "Silently ignore all of them",
          "Guess the user's intent",
          "Forward them into the prompt",
          "Reject or explicitly transform them according to the versioned contract"
        ],
        "answerIndex": 3,
        "explanation": "Silent omission creates hidden behavioral differences and unsafe assumptions."
      },
      {
        "id": "q-serving-contract-5",
        "prompt": "Which case belongs in a conformance suite?",
        "choices": [
          "Only a successful short response",
          "Normal, boundary, invalid, unauthorized, streaming, timeout, overload, restart, and recovery behavior",
          "Only a health check",
          "Only vendor examples"
        ],
        "answerIndex": 1,
        "explanation": "The suite must cover the application contract and important failures."
      },
      {
        "id": "q-serving-contract-6",
        "prompt": "What supports a safe runtime cutover?",
        "choices": [
          "Changing every client directly",
          "Reusing a friendly alias without telemetry",
          "The same conformance and evaluation suites, bounded canary, exact identities, thresholds, and tested rollback",
          "Deleting the prior adapter"
        ],
        "answerIndex": 2,
        "explanation": "A controlled comparison and retained rollback make the change reviewable and reversible."
      }
    ]
  },
  "sources": [
    {
      "title": "Inference Protocol V2",
      "url": "https://kserve.github.io/website/docs/concepts/architecture/data-plane/v2-protocol",
      "publisher": "KServe",
      "lastVerified": "2026-07-27"
    },
    {
      "title": "OpenAI-Compatible Server",
      "url": "https://docs.vllm.ai/en/latest/serving/openai_compatible_server/",
      "publisher": "vLLM",
      "lastVerified": "2026-07-27"
    },
    {
      "title": "Configure Liveness, Readiness and Startup Probes",
      "url": "https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/",
      "publisher": "Kubernetes",
      "lastVerified": "2026-07-27"
    },
    {
      "title": "Chat Completions API Reference",
      "url": "https://developers.openai.com/api/reference/resources/chat/subresources/completions/methods/create",
      "publisher": "OpenAI",
      "lastVerified": "2026-07-27"
    }
  ]
}
