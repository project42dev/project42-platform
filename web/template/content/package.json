{
  "name": "{{NAME}}-content",
  "version": "0.1.0",
  "description": "Curriculum for {{ORGANIZATION}}: the Project 42 canonical curriculum, plus this organisation's own modules.",
  "private": true,
  "type": "module",
  "license": "CC-BY-4.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "content:sync": "node scripts/sync-upstream.mjs",
    "content:check": "node scripts/sync-upstream.mjs --check",
    "content:build": "node scripts/build-catalog.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run content:check && npm run content:build && npm test"
  },
  "dependencies": {
    "@project42/platform": "github:project42dev/project42-platform#v{{PLATFORM_VERSION}}"
  }
}
